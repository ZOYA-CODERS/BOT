{
  "name": "secure-firebase-chat",
  "version": "1.0.0",
  "description": "A secure and beautiful Firebase chat room with authentication",
  "scripts": {
    "start": "live-server --port=3000 --host=localhost",
    "build": "echo \"Build complete\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase": "^9.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "keywords": [
    "firebase",
    "chat",
    "authentication",
    "security"
  ],
  "author": "Your Name",
  "license": "MIT"
}
