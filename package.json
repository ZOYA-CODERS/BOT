{
  "name": "firebase-chat-app",
  "version": "1.0.0",
  "description": "Simple Firebase chat room with login system",
  "scripts": {
    "start": "live-server --port=3000"
  },
  "dependencies": {
    "firebase": "^9.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
